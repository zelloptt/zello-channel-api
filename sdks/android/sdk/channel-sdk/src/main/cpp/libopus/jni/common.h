#ifndef _COMMON_H_
#define _COMMON_H_

#include <malloc.h>
#include <string.h>
#include <pthread.h>

#ifndef max
#define max(a, b) (((a) > (b)) ? (a) : (b))
#endif
#ifndef min
#define min(a, b) (((a) < (b)) ? (a) : (b))
#endif

#endif
